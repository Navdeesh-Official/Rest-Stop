export const MAP_DEFAULT_CENTER = { lat: 40.7128, lng: -74.0060 }; // NYC
export const MAP_DEFAULT_ZOOM = 13;

// OSRM Public API
export const OSRM_API_BASE = 'https://router.project-osrm.org/route/v1';

// Nominatim for Geocoding
export const NOMINATIM_API_BASE = 'https://nominatim.openstreetmap.org';

// Overpass API for Places
export const OVERPASS_API_BASE = 'https://overpass-api.de/api/interpreter';

// Max distance from route to search for toilets (in meters)
export const SEARCH_RADIUS_METERS = 1000; 

// Icons configuration can go here if we were using custom SVG icons for Leaflet
